# Hermite backend — local transcription service.
# hermite-core is installed from the sibling folder:  pip install -e ../core[local]
fastapi>=0.111
uvicorn>=0.30
python-multipart>=0.0.9
faster-whisper>=1.0
huggingface_hub>=0.23

# dev/test
pytest>=8
httpx>=0.27
